import Center from '@/components/center/Center';
import TodoList from '@/components/todo-list/TodoList';

const Page = () => {
  return (
    <Center>
      <TodoList />
    </Center>
  );
};

export default Page;
